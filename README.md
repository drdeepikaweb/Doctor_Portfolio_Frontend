# Dr. Deepika Bhardwaj Frontend

Next.js 16 App Router frontend for a medical portfolio and consultation website.

## Setup

1. Copy `.env.example` to `.env.local`.
2. Set `NEXT_PUBLIC_API_URL` to the backend API URL.
3. Install dependencies.
4. Run `npm run dev`.

Frontend URL: `http://localhost:3000`
